// Package docsync держит сверку документации с кодом.
//
// Продакшн-кода здесь нет и не должно быть: пакет существует ради тестов,
// которые читают README.md и сравнивают его утверждения с тем, что есть в
// исходниках. Отдельный пакет, а не файл в mail или bus, по двум причинам:
// сверка касается сразу нескольких пакетов, и ей нужен путь до корня
// репозитория, который из библиотечного пакета выглядел бы случайным.
package docsync
